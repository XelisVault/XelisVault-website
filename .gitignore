# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/

# Local development artifacts — never commit these
/upload/
/tool-results/
/worklog.md
/scripts/*.png

# Local research/reference clones (XSWD protocol verification)
/.zscripts/
dev.log
worklog.md
/agent-ctx
# Local QA / generation scratch — never commit
/scripts/gen-img-tmp/
/scripts/qa-shots/
/scripts/vlm-*.json
# root-level scratch screenshots (agent-browser saves there)
/door-*.png
/door-*.webm
/qa-img-*.png
/audit-*.png

# nerva raw photo sources (processed duotones are committed)
public/images/nerva/raw/

# runtime artifacts (Prisma dev db)
/db/*.db
/db/*.db-journal

# PDF test output
scripts/gen-img-tmp/

# Axon source: local reference material for the ANTUMBRA study, not site code
axon-src/

# Antumbra repository (separate git repo, root only)
/antumbra/
/scripts/antumbra-wp-en/main.pdf
